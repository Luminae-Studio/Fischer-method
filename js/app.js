// ================================================
// FISCHER METHOD — app.js
// Inicializa o app apos todos os scripts carregarem
// ================================================
document.addEventListener('DOMContentLoaded', function() {
  initAuth();
});
